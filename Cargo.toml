[package]
name = "chip8"
version = "0.1.0"
authors = ["Toni Le <toni.le@outlook.de>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[lib]
name = "chip8"
path = "src/lib.rs"

[dependencies]
